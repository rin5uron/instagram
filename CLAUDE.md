# Claude へのルール

> **「ルール」「ルールを追加して」「ルールを見て」はすべてこのファイル（CLAUDE.md）を指す。**

## 行動ルール

- **書き込みは許可を取ってから。** 新規ファイル・既存ファイル・issue、どこへの書き込みも同じ。書く前に「①何を②どこに書くか」を明示してユーザーの明確なYESをもらう。YESが来るまで書かない。
- **ユーザーの言葉をそのまま記録する。** issueや文書に記録するとき、Claudeの推論・解釈・補足を勝手に加えない。ユーザーの意見を聞いてから反映する。
- **新しいルールはこのファイルに追記する。** ルールが変わったらCLAUDE.mdを更新する。
- **プロジェクト管理はissue駆動型。** 情報はできる限りissueに集約し、ファイルは極力作らない。

## issueへの振り分け方・探し方

- issueに振り分けルールをCLAUDE.mdには書かない（issueは増えるため）
- 振り分け時は `gh issue list` で既存issueを確認し、タイトル・本文から候補を提案する
- **必ずユーザーが最終決定する。Claudeが勝手に振り分けない。**
- issueを探すとき、ラベル検索（`--label`）やキーワード検索（`--search`）を先に使う。全件一覧だけで「ない」と判断しない。

## リサーチissueのルール

- **1検証テーマ = 1issue**
- issue本文に「目的・仮説」を書いてから始める
- 検証記録はコメントで積み上げる
- 検証完了したらcloseする

## 学習ソースについて

- ユーザーが共有するYouTubeリンクは、特に断りがない限りサロン（インスタ運用サロン）が配信している動画
- 学習記録のissueラベルは `study` + `salon`
